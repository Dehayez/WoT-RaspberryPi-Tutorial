import { default as React } from 'react';

import * as Routes from '../../routes';

import './home.scss';

const HomePage = () => {
	return (
		<div className="home">
			HomePage
		</div>
	);
};

export default HomePage;
